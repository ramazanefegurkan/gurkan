# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? |
|---|------|-------|----------|--------|-----------|------------|
| D001 | M001 | arch | Backend framework | ASP.NET Core Web API (controller-based) | Kullanıcı tercihi. Type safety, EF Core + PostgreSQL, iyi performans. | No |
| D002 | M001 | arch | Frontend framework | React + Vite + TypeScript | Kullanıcı tercihi. Component tabanlı, büyük ekosistem, iyi DX. | No |
| D003 | M001 | arch | Database | PostgreSQL | İlerde SaaS dönüşümü için uygun, JSON desteği, multi-currency raporlama. | No |
| D004 | M001 | arch | API authentication | JWT with refresh tokens | Ayrı frontend+backend mimarisi için standart. Email+şifre login. | Yes — social login eklenirse |
| D005 | M001 | arch | Erişim modeli | Grup bazlı (Superadmin → Grup Admin → Üye) | Mülkler gruplara, kullanıcılar gruplara atanır. Aile içi kullanım için esnek. | No |
| D006 | M001 | arch | Dosya storage | Lokal filesystem | Basitlik. Production'da Docker volume mount. İlerde object storage'a geçilebilir. | Yes — S3/MinIO gerekirse |
| D007 | M001 | convention | Multi-currency | TL + USD + EUR, kur dönüşümü yok | Raporlarda her para birimi ayrı gösterilir. Kur dönüşümü karmaşıklık ekler, gerek yok. | Yes — kur dönüşümü istenirse |
| D008 | M001 | arch | Frontend-backend ayrımı | Ayrı projeler, REST API | İlerde mobil app ve SaaS dönüşümü için uygun. Backend bağımsız API surface. | No |
| D009 | M001 | arch | Deploy stratejisi | Docker Compose (API + frontend + PostgreSQL) | Self-hosted VPS. Basit orchestration. | Yes — Kubernetes gerekirse |
| D010 | M001 | scope | Airbnb API | Kullanılmayacak (scope dışı) | Partner-only erişim, public API yok. Kısa dönem veriler manuel girilecek. | Yes — erişim sağlanırsa |
| D011 | M001/S06/T02 | library | Excel export library | ClosedXML 0.105.0 | Free, no native dependencies, good API for generating .xlsx workbooks. Used for per-property income/expense/profit/ROI report export. | Yes |
| D012 | M001/S06/T02 | library | PDF export library | QuestPDF 2026.2.3 (Community license) | Fluent C# API for PDF generation. Community license is free. Requires native Skia DLL — wrapped license init in try-catch for test hosts where native lib can't load. | Yes |
| D013 | M001/S06/T01 | architecture | Notification generation approach | Query-time computation, no persisted notification entities | Notifications are computed fresh on each GET /api/notifications request — no DB table, no migration. Triggers: late rent (DueDate+5 < now), upcoming/overdue bills (7-day window), lease expiry (30/60/90 day tiers), rent increase approaching. Simpler than background jobs or persisted state for the current in-app-only scope. | Yes — if email/push notifications (R019) are added, persistent notification state will be needed |
| D014 | M003 | arch | Mobile framework | React Native + Expo (managed workflow) | Mevcut React + TypeScript bilgisi transfer edilebilir. Expo 2026'da olgun platform — managed workflow, EAS Build, Push Notifications. Flutter düşünüldü ama Dart öğrenme eğrisi gereksiz. | No |
| D015 | M003 | arch | VPS provider | Hetzner | Uygun fiyatlı (~5-10€/ay), güvenilir, Avrupa datacenter. Docker Compose ile deploy. | Yes — başka provider gerekirse |
| D016 | M003 | arch | HTTPS / reverse proxy | Nginx veya Caddy + Let's Encrypt | Standart pattern. Caddy otomatik HTTPS ile daha basit olabilir. Execution sırasında karar verilecek. | Yes |
| D017 | M003 | arch | Push notification servisi | Expo Push Notifications | Ücretsiz, Expo ekosistemiyle entegre. Device token + push API. | Yes — FCM/APNs direkt kullanılmak istenirse |
| D018 | M003 | scope | Import kapsamı | Kısmi — sadece Airbnb CSV + geçmiş kira ödemeleri | Gider/fatura import'u şimdilik yok. Tam import daha sonra eklenebilir. | Yes — gider/fatura import istenirse |
| D019 | M003/S01 | infra | Reverse proxy for production deploy | Caddy 2 (not Nginx) as reverse proxy | Caddy provides zero-config automatic HTTPS via Let's Encrypt — no manual certbot setup, renewal cron jobs, or certificate path configuration. Simpler Caddyfile syntax vs nginx.conf. D016 left this open between Nginx and Caddy; resolving in favor of Caddy based on single-VPS Docker Compose deployment simplicity. | No — already integrated into Caddyfile and docker-compose.prod.yml |
| D020 | M003/S02 | frontend | CSS architecture for shared component styles | Single shared.css file imported by all pages, with page-specific CSS files kept alongside their components | Eliminated 18+ cross-page CSS imports (pages importing Properties.css and Tenants.css for shared classes). Shared classes (buttons, forms, tables, tabs, badges, feedback states, dialogs) now live in one file. Reduced Properties.css by ~50% and Tenants.css by ~50%. CSS bundle size decreased slightly (38.97→38.82 KB gzip) due to deduplication. | Yes — if migrating to CSS modules or Tailwind |
| D021 | M003/S03 | backend | CSV parser instantiation approach | Plain classes instantiated directly in controller, not DI-registered services | Parsers have no shared state and no dependencies beyond what's passed to ParseAsync (DbContext, userId, etc.). DI registration would add ceremony without benefit. If parsers gain dependencies later, they can be promoted to DI services. | Yes |
| D022 | M003/S04 | architecture | Mobile JWT token storage mechanism | expo-secure-store for native (encrypted keychain), localStorage fallback for web | SecureStore encrypts tokens in the device keychain (iOS Keychain / Android Keystore). Never stored in AsyncStorage or plain text. localStorage fallback enables Expo web development. Pattern matches security best practices for mobile JWT storage. | Yes |
| D023 | M003/S04 | architecture | API client ↔ auth context dependency direction | Duplicate 2 small SecureStore helper functions in client.ts rather than importing from ctx.tsx | ctx.tsx imports from client.ts (login function, types). If client.ts also imported from ctx.tsx, it would create a circular dependency. The duplicated helpers are ~10 lines total. ctx.tsx → client.ts is the canonical dependency direction. | Yes |
| D024 | M003/S05 | architecture | Expo Router route structure for property sub-pages | Flat routes with query params (e.g., `/(tabs)/properties/tenants?propertyId=xxx`) instead of nested dynamic segments (`/(tabs)/properties/[id]/tenants`) | Expo Router nested dynamic segments (`[id]/tenants.tsx` inside a directory that already has `[id].tsx`) can cause route configuration issues. Flat routes with `propertyId` passed as a query param via `router.push({ pathname, params })` are simpler, more predictable, and avoid Stack navigator nesting complexity. The S05 research doc identified this tradeoff and recommended the flat approach. | Yes — if Expo Router improves nested dynamic route support |
| D025 | M003/S06 | architecture | Notification computation extraction pattern | Extract NotificationsController.Get() logic into INotificationComputeService, consumed by both GET /api/notifications and POST /api/push/trigger | Both the in-app notification query endpoint and the push trigger need identical computation logic (late rent, upcoming bills, lease expiry, rent increases). Extracting to a shared service avoids duplication and ensures push notifications always match in-app notifications. D013 noted query-time computation — this preserves that pattern while enabling push delivery. | Yes |
