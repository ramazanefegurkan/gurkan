S06 delivered the final three capabilities for M001: financial dashboard with multi-currency aggregation (GET /api/dashboard), query-time in-app notifications for late rent/upcoming bills/lease expiry/rent increases (GET /api/notifications), and Excel/PDF report export with per-property ROI (GET /api/reports/profit-loss, /export/excel, /export/pdf). All endpoints enforce group-based access control. 16 S06 integration tests pass, 77/77 full regression. Frontend Dashboard page with summary cards per currency, per-property breakdown table, and export buttons. Notifications page with severity-colored cards. Default route changed to /dashboard. Key decisions: ClosedXML for Excel (D011), QuestPDF for PDF (D012), query-time notification computation with no DB persistence (D013).